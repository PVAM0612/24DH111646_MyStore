@model _24DH111646_MyStore.Models.Category

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2 class="bg-success">XÓA DANH MỤC</h2>

@using (Html.BeginForm())
    {
     <div class="form-horizontol ">
         <hr />
         <div class="col-md-offset-2 col-md-10">
             <h4>bạn chắc chắn muốn xóa danh mục này chứ?</h4>
         </div>
         <div class="form-group">
                 <div class="control-label col-md-2">Tên danh mục</div>
                 <div class="col-md-10">
                     @* Hiển thị giá trị của Category *@
                     @Html.DisplayFor(model => model.CategoryName)
                 </div>
         </div>
             @Html.AntiForgeryToken()

          <div class="form-group">
                 <div class="col-md-offset-2 col-md-10">
                     <input type="submit" value="Xóa" class="btn btn-primary" />
                     @Html.ActionLink("Quay lại danh sách", "Index", null, new { @class = "btn btn-primary" })
                 </div>
          </div>
     </div>

 }

